---
name: dev
description: Start the dev server and open it for visual iteration
disable-model-invocation: true
allowed-tools: Bash
---

# Start Dev Server

Start the Next.js development server for visual iteration.

```bash
npm run dev
```

The dev server runs at http://localhost:3000. Use this when iterating on design changes. The server hot-reloads on file changes.
